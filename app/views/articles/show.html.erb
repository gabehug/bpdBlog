<!DOCTYPE html>
<html>
  <head>
  <title>Article</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
  </head>

  <body class="prose mx-auto">
    <div class="">
    <h1><%= @article.title %></h1>
<p><%= @article.body %></p>

<ul>
  <li><%= link_to "Edit Article", edit_article_path(@article) %></li>
  <li><%= link_to "HOME PAGE", articles_path %></li>
  <li><%= link_to "Erase Article", article_path(@article), data: {
    turbo_method: :delete,
    turbo_confirm: "Are you sure?"  
  }%></li>
</ul>
    </div>
  </body>
</html>
